---
title: 本机PDF发布功能 |设计页面布局
description: 了解如何设计页面布局，以在PDF输出的不同部分中显示信息。
hide: true
hidefromtoc: true
exl-id: b4d3bdc4-0d01-46eb-b182-540380220485
source-git-commit: f8af7d7180b3d70d17e1410885017212dec721ef
workflow-type: tm+mt
source-wordcount: '4688'
ht-degree: 0%

---


# 设计页面布局

在创建PDF文档时，您会有不同的部分来显示不同类型的信息。 例如，PDF文档将从头页或封面开始，该页面将包含您公司的徽标、书籍标题或版本信息。 然后会有章节、附录或术语表页面。 PDF文档中的每个部分看起来都不同，这是通过创建和自定义页面布局来实现的。

设计页面布局时，您可以定义构成页面的各种元素。 例如，您可以定义页面上的页面大小、边距、页眉和页脚、方向和其他页面规范。 本机PDF发布功能允许您根据 [页面媒体标准](https://www.w3.org/TR/css-page-3/). 使用本机PDF发布功能的用户界面，可以轻松自定义页面媒体标准涵盖的大多数设置。 对于某些其他高级格式，您可以使用“源”视图编写您自己的CSS代码。

设计页面布局后，您需要将这些布局与“PDF页面布局”设置中的相应部分相关联。 请参阅 _创建和自定义页面布局_ 部分以了解有关如何创建和打开页面布局以进行自定义的详细信息。

## 页面布局类型

PDF文档通常包含以下部分：

* 封面
* 目录
* 数字提升
* 表格提升度
* 章节或主题页面
* 术语表
* 索引
* 后页

这些部分需要相应的页面布局才能以特定格式显示信息。 此外，您还可以有一个空白页面，用作从奇数或偶数页面开始新章节的填充符。 在这种情况下，您可以使用默认页面布局或为空白页面创建页面布局。 请参阅 _创建新页面布局_ 以了解更多详细信息。

位于 **模板>设置** 部分，用于定义要用于PDF不同部分的页面布局。 每个页面布局还可以具有不同的首页、右页或左页变体。

### 创建第一个、右侧或左侧页面布局变体

通过具有不同的首页、右页或左页布局变体，可以进一步自定义PDF模板中的不同页面布局。 您可以使用页面布局设计器以不同方式设计这些页面。

>[!NOTE]
>
>如果您想为书籍中的某个部分设置单个页面布局，则无需创建“首页”、“右页”或“左页”布局。

创建页面布局时请考虑以下几点：

>[!NOTE]
>
>以下几点以章节页面布局为例。 但是，这些点也适用于其他页面布局。

* 如果要对章节中的所有页面使用单个页面布局，则应仅创建单个章节页面布局，而不使用任何变体。

* 如果您想对书籍中各章的首页有不同的外观，则需要为各章创建一个“首页”布局变体。

* 如果您希望对书籍的每个左侧和右侧页面具有不同的外观，则需要为章节页面布局创建左侧和右侧变体。

* 如果希望章节从奇数或偶数页面开始，则可以选择为空白页面创建页面布局。 此页面布局用于填充两个章节之间的间隙，以确保章节从所需的奇偶页面开始。

   >[!NOTE]
   >
   >如果不创建单独的空白页面布局，则使用默认的页面布局。 要创建页面布局，请参阅 _创建新页面布局_.

以下示例将指导您完成创建页面布局变体的过程：

1. 使用“创建新页面布局”过程中提供的步骤创建“章节”页面布局。

   将在“页面布局”下创建并添加空白的章节页面布局。

   默认情况下，创建页面布局时，也会打开进行编辑。 以下屏幕截图显示一个空白（默认）的页面布局：

   <img src="./assets/default-blank-page-layout.png" width="300">

   默认情况下，会创建模板中的页眉、页脚和内容区域。 您可以使用用户界面中给定的页面属性、内容属性和各种工具（如插入图像、字段等）轻松自定义这些区域。

   >[!NOTE]
   >
   >对于高级配置，您可以使用“源”视图并添加自定义HTML和CSS代码。

1. 将鼠标悬停在 **章节** 布局，然后单击 **选项** 以显示上下文菜单。

1. 单击或将鼠标悬停在 **添加布局变体** 并选择要创建的所需页面布局（“首页”、“左页”或“右页”）。

所选页面布局是使用基本章节布局的副本创建的。 这意味着，如果您在默认的章节页面布局中进行了任何更改，则在创建页面布局时，变体页面布局中会复制相同的更改。

## 使用页面布局的页面属性

在设计页面布局时，控制各种页面属性至关重要。 本机PDF发布功能将所有主要页面属性封装在页面属性面板下。 “页面属性”面板提供了对以下部分下的各种属性的访问权限：

>[!NOTE]
>
>页面属性面板封装属性，并遵循 [页面媒体标准](https://www.w3.org/TR/css-page-3/).

* **页面大小** :指定要用于页面布局的页面大小。 “页面大小”下拉列表允许您从15个以上的页面大小中进行选择。 您还可以使用自定义页面大小创建页面布局，请参阅 **设置页面大小** 以了解更多详细信息。

* **方向** :指定用于页面布局的页面方向。 您可以从“纵向”或“横向”页面方向中进行选择。 请注意，您可以选择在页面布局中将不同的方向应用于不同的页面变体。 例如，如果您的内容包含宽表格或大图像，则可以创建横向页面布局并将该布局应用于较宽的表格或图像。

* **视图旋转** :指定旋转后原始顶侧所在的侧面或方向。 可以选择顺时针90度、逆时针90度或逆时针180度。 当您想在输出中使用纵向和横向布局的组合时，此选项非常有用。 例如，您可以使用纵向作为通用页面布局，并且可以设置用于渲染宽表格的横向页面布局。 在这种情况下，可以设置为顺时针90度显示表内容。 这样，页面将以横向方向定向，内容将旋转90度以保持视图的连续性。 我们将在本节的后面部分作为示例来查看如何实现此目标。

* **重新开始编号** :指定此页面布局的编号将从中开始的页码。 例如，您可以设置每个章节要重新启动的页码。 在这种情况下，您需要在章节页面布局的“第一个页面布局”变体上将“重新开始从编号”属性设置为1。

* **布局** :指定页边距以及上边距、下边距、左边距和右边距。 下图说明了如何在内容周围呈现边距、内边距和边框。 请注意，页面顶部和底部的边距包含页眉和页脚。

   <img src="./assets/margins-padding-illustration.png" width="300">

* **背景** :包含图像或颜色作为页面布局的背景。 对于图像，您可以指定图像的高度和宽度，以及重复和位置属性。

* **脚注** :指定要在输出中显示脚注的属性。 您可以选择指定边距和内边距属性以及边框样式。

### 设置页面大小

在页面布局中，您首先需要定义的是页面大小。 在“页面属性”中，您可以为页面布局选择超过15个页面大小。 您还可以通过执行以下步骤来创建自定义页面大小：

1. 打开所需的页面布局进行编辑。

   >[!NOTE]
   >
   >请参阅 _自定义页面布局_ 部分，用于打开页面布局进行自定义或编辑。

1. 在右侧面板中，单击 **页面属性**.
1. 在 **页面大小** 下拉列表中，选择 **自定义**.

   将显示“页面宽度”和“页面高度”字段。

1. 在 **页面宽度** 和 **页面高度** 字段。

   >[!NOTE]
   >
   >一些最常用的单位是px（像素）、pt（点）、rem、em、%（百分比）和（英寸）。

### 使用页面方向和视图旋转

让我们看一个示例，其中使用了纵向和横向页面方向以及视图旋转属性的组合。 在本例中，我们将创建一个默认纵向方向的PDF，但表格将以横向呈现，内容将以顺时针90度视图显示。 最终输出将类似于：

<img src="./assets/portrait-landscape-page-layouts.png" width="400">

在以上输出中，联系人列表信息以横向模式显示，内容也旋转了90度。 其余内容以普通纵向模式显示。

要实现这种输出，需要完成以下主要任务：

1. 创建具有横向的页面布局。

1. 更改 **视图旋转** 用于在90°中渲染内容的属性。

1. 创建自定义样式以使用新页面布局。

1. 在要在横向页面布局中呈现的表的输出类定义中添加样式。

执行以下步骤以完成上述任务：

1. 创建具有横向的页面布局。
   1. 使用“创建新页面布局”步骤下给出的步骤创建“横向”页面布局。

   1. 在右侧面板中，单击 **页面属性**.

      <img src="./assets/page-properties-panel.png" width="300">
   1. 更改 **方向** to **横向**.

1. 更改“视图旋转”属性，以顺时针方向90°呈现内容。

   1. 选择 **顺时针90°** 从查看旋转下拉列表中。

   <img src="./assets/view-rotation-page-props.png" width="300">

   1. 单击 **全部保存** 以保存更新的页面布局属性。

1. 创建自定义样式以使用新页面布局。
   1. 展开左侧边栏，并双击要在其中创建样式的模板。

   1. 展开样式表部分。

   1. 将鼠标悬停在布局样式表上，然后单击&#x200B;_选项_ 图标)。..并选择“编辑”。

      将打开布局样式表进行编辑。

   1. 右键单击 **其他样式** 选择 **新样式**.
      <img src="./assets/stylesheet-other-new-style.png" width="300">

   1. 在“添加样式”弹出窗口中，输入 **横向样式** 在 **classname**.
      <img src="./assets/stylesheet-new-landscape-style.png" width="400">

   1. 单击&#x200B;**完成**。

      一种名为 `.landscape-style` 将在“其他样式”列表的末尾创建并添加。

   1. 双击 `.landscape-style` 样式进行编辑。

   1. 展开 **分页** 属性。

   1. 输入 `Landscape` 在 **页面布局** 属性。

      <img src="./assets/new-style-with-landscape-layout.png" width="500">

   1. 单击 **全部保存** 以保存更新的样式属性。

1. 在 `outputclass` 要在横向页面布局中呈现的表的定义。
   1. 在DITA文件编辑器中，打开要应用新页面布局的文件。

   1. 查找 `<table>` 元素，以横向模式呈现。

   1. 在痕迹导航中，单击表元素以选择表。

      <img src="./assets/new-style-table-element.png" width="400">

   1. 在右侧面板中，单击并打开“内容属性”面板。

   1. 在“内容属性”面板中，添加新 **outputclass** 属性 **横向样式** 作为属性值。

      <img src="./assets/new-style-table-outputclass.png" width="300">

1. 单击 **全部保存** 以保存更新的文件。
1. 生成PDF输出。

最终PDF将以横向模式呈现表内容，如示例开头所示。

### 添加背景图像

根据您的要求，您可能需要添加一个背景图像，该图像显示在章节(PDF)输出的每个首页。 通过“页面属性”下的“背景”属性，您可以轻松添加背景图像。 您可以选择在一个页面中复制此图像，并将该图像放置在页面顶部、底部或中心区域的任意位置。

例如，要在内容区域的中心部分插入背景图像，请执行以下步骤：

1. 打开所需的页面布局进行编辑。

   >[!NOTE]
   >
   >请参阅 _自定义页面布局_ 部分，用于打开页面布局进行自定义或编辑。

1. 单击内容区域中的任意位置。

1. 在右侧面板中，单击 **页面属性**.

1. 展开 **背景** 中。

1. 单击 **图像路径** 位置字段。

1. 浏览并选择您要用作背景图像的图像。

   插入并复制图像以覆盖整个页面。

1. 通过调整高度和宽度属性来更改图像大小。

   >[!NOTE]
   >
   >您可以输入高度或宽度属性，因为图像会自动缩放以保持宽高比。

1. 设置其他属性以调整您希望背景图像的显示方式。

   * **背景重复** :指定是否希望背景重复。

   * **背景位置** :指定页面上背景图像的位置。

以下屏幕截图显示背景图像，其中“背景重复”属性设置为 _无重复_ 和“背景位置”属性设置为 _中心中心_.

<img src="./assets/background-image.png" width="500">

## 使用页眉和页脚

当您在页面布局的页眉或页脚中包含信息时，该信息会在使用该页面布局的所有页面上重复。 通常，页眉区域用于章节或主题标题，页脚区域用于显示页码。

创建新页面布局时，默认创建页眉和页脚区域。 您可以在页面布局的页眉和页脚区域中进行许多自定义设置。 例如，您可以插入图像（如徽标）、变量（包含动态信息）或静态内容。

### 更改页眉和页脚边距和行

默认情况下，页眉和页脚边距设置为1英寸。 您可以通过更改“页面属性”面板中的“边距”设置来更改此默认值。 执行以下步骤以更改页眉和页脚大小：

1. 打开所需的页面布局进行编辑。

   >[!NOTE]
   >
   >请参阅 _自定义页面布局_ 部分，用于打开页面布局进行自定义或编辑。

1. 在右侧面板中，单击 **页面属性**.
1. 展开 **布局** 中。
1. 单击 **边距** 属性。
1. 要更改标题大小，请在“上边距”字段中输入所需值。

   >[!NOTE]
   >
   >一些最常用的单位是px（像素）、pt（点）、rem、em、%（百分比）和（英寸）。

1. 要更改页脚大小，请在“下边距”字段中输入所需的值。

您可以设计页眉和页脚区域以包含多行。 为此，请添加\&lt;p> 标记(使用插入HTML元素)<img src="./assets/insert-html-element-2.svg" width="25">)图标。

| _开发人员角_: <img src="./assets/developer-corner-icon.svg" width="25"> |
|---|

如果您希望直接使用CSS和HTML代码，则可以更改边距值，如以下代码片段中所示：

```css
…

<meta name="page-style" content="size:A4 portrait;margin-top:3cm;margin-right:30pt;margin-bottom:1in;margin-left:90px;" />

…
```

>[!NOTE]
>
>在上例中，使用不同的单位来指定边距值。

### 删除页眉和页脚

页眉和页脚叠加在顶部和底部边距中。 从技术上讲，这意味着如果要在页面布局中保留页眉和页脚，则必须在顶部和底部边距中保留所需的空间。

如果您不希望页面布局具有页眉和页脚，则可以通过两种方式来实现此目的：

* 如果要保留上边距和下边距，请将页眉和页脚区域留空。
* 如果您不想保留上边距和下边距（如设计杂志的前封面和后封面），则可以通过将上边距和下边距属性设置为0来删除边距。 这样就没有页眉和页脚的空间。

### 在标题中添加图像或徽标

根据您的要求，您可能需要添加一个显示在页面布局的标题区域（或任何其他部分）中的图像。 可以通过两种方式在页面布局中添加图像：

* 使用模板资源中的图像。
* 使用\&lt;add image=&quot;&quot;> 工具。

>[!NOTE]
>
>建议使用“资源”文件夹管理所有模板资产，如图像或字体。

要在标题区域中插入类似贵公司徽标的图像，请执行以下步骤：

1. 打开所需的页面布局进行编辑。

>[!NOTE]
>
>请参阅 _自定义页面布局_ 部分，用于打开页面布局进行自定义或编辑。

1. 单击编辑标题(<img src="./assets/header-icon.svg" width="25">)图标将光标置于标题区域中。

   或者，在标题区域内单击。

1. 要添加图像，请选择以下任一方法：
1. 单击 **惰性图像** (<img src="./assets/insert-image-icon.svg" width="25">)图标；在 **选择路径** 弹出窗口，浏览到图像位置并单击 **选择** 以将其插入标题区域。
1. 将图像从Resources文件夹拖放到标题区域。

以下屏幕截图显示了在标题区域中添加的示例图像。

<img src="./assets/image-in-header-area.png" width="500">

插入图像后，您可以修改其属性以赋予其所需的外观。 更改页面布局中图像或任何其他元素外观的最简单方法是，使用“内容属性”面板。 请参阅 _使用“内容属性”面板_ 用于通过UI进行自定义的各种属性。

### 添加字段和元数据

当您想要插入一段预定义的信息时，字段非常有用。 例如，您可以在章节标题区域中包含“章节标题”字段，该字段在发布时将替换为实际的章节标题。

可以在页面布局中插入的字段类别如下：

* 日期
* 用时
* 主题标题
* 项目标题
* 页码
* 总页面数
* 章节标题
* 章节编号
* 元数据

每个字段类别都包含可插入字段信息的不同变体。 例如，日期字段可以具有不同的变体，如 `YYYY-MM-DD`, `MM/DD/YY`, `MM/DD/YYYY` 等等。 同样，页码也可以具有罗马、小数甚至区域设置特定格式的变体，例如 _阿拉伯语_, _梵文_, _希伯来语_，等等。

除了预定义的字段之外，您还可以将元数据信息添加为页面布局中的变量或字段。 此元数据存储在源DITA映射内容中，并且可以轻松插入到页面布局中。 有关更多信息，请参阅 _使用字段和元数据_.

在以下示例中，我们将在页面布局的页脚区域插入一个页码和一个章节标题。

1. 打开所需的页面布局进行编辑。

   >[!NOTE]
   >
   >请参阅 _自定义页面布局_ 部分，用于打开页面布局进行自定义或编辑。

1. 单击 **编辑页脚** (![](./assets/footer-icon.svg))图标将光标置于页脚区域。

   或者，在页脚区域内单击。

1. 通过单击 **插入HTML元素** (<img src="./assets/insert-html-element-2.svg" width="25">)图标，并从元素列表中选择“段落”。

1. 单击 **插入字段** (![](./assets/insert-fields-icon.svg))图标。

   此时会出现“字段”弹出窗口。

1. 选择 **页码** 类别， **default(1)** 页码格式，然后单击 **插入**.

   <img src="./assets/insert-page-number-field.svg" width="400">

   >[!NOTE]
   >
   >您还可以编辑所有字段的格式（默认格式除外）。 为此，请单击要编辑的格式旁边的编辑图标，进行更改，然后单击确定。 有关更多信息，请参阅 _使用字段和元数据_.

   默认的页码字段会插入到页面布局的页脚区域。

   <img src="./assets/page-number-field-in-footer.png" width="400">

   顶部的痕迹导航列出了存储信息的元素。

1. 在页码字段后面输入空白，然后单击 **插入字段** 图标。

1. 选择 **章节标题** 类别， **章节标题** 格式，然后单击 **插入**.

   的 _章节标题_ 字段，该字段在发布时填充了章节标题，并会插入到页脚区域。 此时，页码和章节标题字段以空格分隔。

   <img src="./assets/page-number-topic-title-near-footer.png" width="400">

1. 要右对齐章节标题，请执行以下步骤：

   1. 单击痕迹导航上的字段元素以选择章节标题字段。

   1. 在右侧面板中，单击 **内容属性** (<img src="./assets/content-properties-icon.png" width="25">) 图标.

   1. 展开 **布局** 属性部分，并设置 **浮动** 属性值为 **右**.
      <img src="./assets/float-prop-html-content.png" width="400">

      章节标题字段与页脚的右侧对齐。
      <img src="./assets/topic-title-moved-right-footer.png" width="500">


| _开发人员角_: <img src="./assets/developer-corner-icon.svg" width="25"> |
|---|

如果您希望直接使用CSS和HTML代码，则还可以通过转到页面布局的“源”视图并在代码中进行更改来实现此目的。 以下代码片段显示了通过代码完成的相同页脚设置：

```css
…
<p>

<span data-field="page-number" data-format="default">1</span>

<span data-field="chapter-title" data-format="default" style="float: right">Chapter Title</span>

</p>
…
```

## 使用内容区域

就内容空间而言，内容区域是最大的区域。 内容区域会填充您主题的内容。 在某些特殊情况下，您可以在内容区域中添加样板内容。 此内容是在页面布局中的指定位置发布的。 例如，目录、术语表和索引中的标题可以添加为样板内容，该样板内容将在最终输出中“按原样”发布。 另一个示例是章节目录，通常在每个章节的第一页中添加。

内容区域中最常用的自定义设置之一是多列布局。 借助功能强大的页面布局设计器，您可以自定义特定页面以在多列中呈现，同时将其他页面中的内容保留在一列中。

在以下部分中，我们将介绍各种情景以自定义内容区域。

### 添加章节目录

章节目录可作为读者快速了解章节内容的参考。 通常，在章节的开头处添加一个章节目录。 因此，如果要使用章节目录，则可以将其添加到主章节页面布局的内容区域或章节的第一个页面布局变体中。

在以下示例中，我们将在章节的第一个页面布局中插入一个章节目录：

>[!NOTE]
>
>对于此过程，假定您为章节页面布局创建了第一个页面变量。 有关如何创建页面变体的说明，请参阅 _创建第一个、右侧或左侧页面布局变体_.

1. 打开所需的页面布局进行编辑。

   >[!NOTE]
   >
   >请参阅 _自定义页面布局_ 部分，用于打开页面布局进行自定义或编辑。

1. 将光标放在页面布局的内容区域中。

1. 单击章节目录(<img src="./assets/chapter-toc-icon.svg">) 图标.

   默认的章节目录会插入到内容区域中。

   <img src="./assets/chapter-toc-default.png" width="400">

   >[!NOTE]
   >
   >默认章节目录包含标题1到4。 在本例中，标题1是章节标题本身。 因此，您可能不希望目录中再次显示章节标题，或者希望提高目录中所需标题的级别。 您可以通过更改属性来自定义目录。

1. 打开“内容属性”面板以自定义目录标题级别。

   例如，如果您要从“标题2”开始，请将第一个下拉列表从2更改为开始。

   <img src="./assets/customize-chapter-toc.png" width="400">

   同样，如果您希望标题一直保持在5级，则将第二个下拉列表更改为5。 更新的目录将如下所示：

   <img src="./assets/chapter-toc-updated.png" width="400">

   >[!NOTE]
   >
   >最终发布的PDF将仅显示基于章节内容的目录条目。 如果章节中没有级别5标题，则不会在最终输出中显示该标题。

可以使用样式表自定义默认目录的外观。 样式以 `chaptoc-level-#` (赞 `chaptoc-level-1`, `chaptoc-level-2`等)用于自定义章节目录的样式。 有关目录中使用的样式表元素以及如何自定义这些样式表元素的更多详细信息，请参阅 _自定义默认章节目录_.

>[!IMPORTANT]
>
>当前，如果您在样式表中进行任何样式更新，则它可能不会反映在内容预览中。 但是，输出将使用更新的样式呈现。

### 使用多列页面布局

多列页面布局在出版杂志或书籍索引中非常常见。 使用本机PDF发布功能，可轻松将文档拆分为多列。 使用不同的页面布局，您可以选择仅将特定部分保留为多列，而将其他部分保留为单列（或普通）布局。

要创建具有多列的页面布局，请执行以下步骤：

1. 打开所需的页面布局进行编辑。

   >[!NOTE]
   >
   >请参阅 _自定义页面布局_ 部分，用于打开页面布局进行自定义或编辑。

1. 由于多列布局应用于内容（不包括页眉和页脚区域），因此您需要在痕迹导航中选择内容元素。

   选择内容痕迹导航后，“内容属性”面板将显示多列的属性。

   <img src="./assets/multiple-columns-properties.png" width="400">

1. 使用多列属性自定义多列页面布局：

   * **列计数：** 指定要划分页面的列数。 使用向上和向下箭头图标或输入一个数字来设置列数。

   * **列宽：** 在多列布局中指定列的宽度。 默认情况下，大小以像素(px)为单位进行设置，您还可以以pt、rem、em、%或单位来指定大小。

      >[!NOTE]
      >
      >如果未指定大小，则将统一划分列以适合给定页面。 在大多数情况下，您无需指定此值。

   * **列间隙** :指定各个列之间的间距。

   * **列范围** :如果您希望页面布局中的任何元素能够跨列显示，则需要使用此资产。 这是通过使用样式表修改所需元素的样式来实现的，有关详细信息，请参阅 _风格定制说明一节_.

   在页面布局中，如果希望某个文本显示在所有章节页面布局的第一页，则可以将其添加到章节页面布局的第一页变体。

   如以下示例所示，标题文本的“跨列”属性设置为“全部”。 这可确保即使文档是多列的，标题也会跨列。

   <img src="./assets/element-span-across-columns.png" width="400">

   >[!IMPORTANT]
   >
   >可以使用outputclass属性将Span Column属性应用于任何DITA元素。

   * **列填充** :指定内容填充列的方式。 默认情况下，该值设置为“余额”，该余额将用相等的内容量填充每列。

   * **列规则** :如果要在列之间插入行，请使用此属性定义行样式或划线样式。 指定规则样式、颜色和宽度的值，以在列之间添加一条线。


## 使用“内容属性”面板

通过“内容属性”面板，您可以轻松更新页面布局中元素的外观。 “内容属性”面板下的属性分为以下几部分：

* **字体** :包含与文本相关的属性。 您可以设置字体系列、粗细、大小、文本修饰（下划线、上线、直线）、文本样式（粗体、斜体等）、文本对齐（左、右、中或两端对齐）、处理空格（预定义格式、无换行、换行空格等）、行高、字母间距和文本缩进。

* **边框** :包含用于向页面布局中的元素添加边框并设置其格式的属性。 您可以设置“边框侧”（全部、顶部、底部、右侧或左侧）、“边框样式”（实线、虚线、虚线等）、“边框颜色”、“宽度”和“半径”，以便具有弯曲的边框。 在以下示例中，在页面的标题区域中添加了一个弯曲的边框。

   <img src="./assets/border-properties.png" width="500">

* **布局** :包含用于在页面布局中配置元素布局的属性。 您可以设置“高度”、“宽度”、“边距和内边距”（适用于顶部、底部、左侧或右侧）、“水平”或“垂直对齐”、“浮动”（适用于左、右、无）、“清除”（适用于左、右、两者或无）、元素的位置（作为绝对、固定、相对或更多）、“显示”（作为块、内容、修复或更多）、“Z索引”、“透明度”、“变换”（通过旋转或缩放）以及“变换原点”（按X和Y偏移）。

* **背景** :包含用于包含背景图像或颜色阴影的属性。 您可以设置“图像大小”（通过设置“高度”或“宽度”）、“背景重复”（作为重复、不重复、倒圆角或更多）和“背景位置”（作为左上、右中心、居中底部或更多）。
* **多列** :包含用于为页面或任何特定元素（如章节目录）配置多列属性的属性。 有关属性及其使用方式的更多详细信息，请参阅 _使用多列页面布局_.
